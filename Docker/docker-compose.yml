services:
  frontend:
    image: node:20-alpine
    working_dir: /app
    ports:
      - "4173:4173"
    volumes:
      - ..:/app
      - /app/node_modules
    command: sh -c "npm ci && npm run dev -- --host 0.0.0.0 --port 4173"
